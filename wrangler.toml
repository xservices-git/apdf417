name = "apdf417"
main = "worker/index.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./web/dist"
binding = "ASSETS"

# Bindings + secrets cấu hình trên Cloudflare Dashboard (GUI):
#  - Settings -> Functions -> KV Namespace Bindings: APDF417_KV
#  - Settings -> Variables and Secrets: ADMIN_USER, ADMIN_PASS, PUBLIC_URL (nếu cần)
